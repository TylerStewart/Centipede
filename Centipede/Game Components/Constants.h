#ifndef _CONSTANTS
#define _CONSTANTS

#include "TEAL\CommonElements.h"


/*const float windowMaxX=480;
const float windowMaxY=512;
const float fieldMinY=24;//this is the top border
const float fieldMinX=8;//center of extreme left grid space.  
const float fieldMaxY=windowMaxX-8;//-8 because its half the width of a grid space
const float fieldMaxX=windowMaxY-8;//same here
const float gridSpaceWidth = 16;*/
const sf::Vector2f CentipedeSpawnCenter = sf::Vector2f(480/2, -40);
const sf::Vector2f ScorpionDebugSpawn = sf::Vector2f(500,100);
//const float gridSpaceWidth = 16;
#endif 